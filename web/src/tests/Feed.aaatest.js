// import FeedPage from "../feedPage/FeedPage.js";

// describe('FeedPage test', () => {
//     it('renders without crashing', () => {
//       render(<FeedPage postList={posts} />);
//     });
  
//     it('displays posts', () => {
//       const { getByText } = render(<FeedPage postList={posts} />);
//       expect(getByText('Post 1')).toBeInTheDocument();
//       expect(getByText('Post 2')).toBeInTheDocument();
//     });
  
//     it('displays search input', () => {
//       const { getByPlaceholderText } = render(<FeedPage postList={posts} />);
//       expect(getByPlaceholderText('Search')).toBeInTheDocument();
//     });
  
//   });